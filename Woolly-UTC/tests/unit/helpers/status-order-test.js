
import { statusOrder } from 'wu/helpers/status-order';
import { module, test } from 'qunit';

module('Unit | Helper | status order');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusOrder([42]);
  assert.ok(result);
});

