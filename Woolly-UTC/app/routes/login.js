import Ember from 'ember';

export default Ember.Route.extend({
//   afterModel: function(model, transition) {
//         var controller = this.controllerFor('application');
//         if (transition.queryParams.ticket) {
//
//             controller.set("message", "Connexion en cours...");
//             console.log("connexion en cours");
//             controller.set("waiting-from-server", true);
//             this.get("session").authenticate("authenticator:cas", transition.queryParams.ticket, window.location.href.split("?")[0]).catch(
//                 (reason) => {
//                     controller.set("waiting-from-server", false);
//                     controller.set("message", reason);
//                 }
//             );
//         }
//
// }
});
